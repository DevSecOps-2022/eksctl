apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop-dev
  region: us-east-1

managedNodeGroups:
  - name: roboshop-dev-ng
    instanceTypes: 
      - t3.small
    desiredCapacity: 2
 

# eksctl create cluster --config-file=eksctl.yaml

