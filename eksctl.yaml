apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop-dev
  region: us-east-1

managedNodeGroups:
  - name: spot
    instanceTypes: 
      - t3.small
    spot: true

# eksctl create cluster --config-file=eksctl.yaml

